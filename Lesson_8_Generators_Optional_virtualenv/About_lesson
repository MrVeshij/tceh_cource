- Генераторы
- код для чисел Фибоначи
- вес объектов
- yield (создание генератора) - инструмент для генератора
- функция iter
- переменная _ (показывает что данная переменная не особо нужна)
- Отличия Python 2 & Python 3
- Python_future это библиотеки для возможности писать код одновременно для Python 2 & 3
- repr это полное отображение записи объекта
- управление зависимостями
- установка virtualenv
- виртуальные среды (как ими пользоваться)
- pycodestyle пакет для проверки стиля кода
- управление зависимостями через pip (requirements.txt)
- gitignore (в данном файле прописываются файлы которые не должны быть в репозитории)
